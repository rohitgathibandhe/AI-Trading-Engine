[pytest]
addopts = -ra
testpaths = tests
markers =
    unit: fast unit tests for isolated components
    integration: end-to-end or multi-module tests
    backtest: slower backtest or simulation smoke tests
